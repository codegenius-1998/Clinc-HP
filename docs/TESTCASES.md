# Clinc-HP テストケース仕様書

**対象システム**：Clinc-HP（AIクリニックホームページ自動生成・編集システム）
**最終更新**：2026-08-23
**関連文書**：[SPEC.md](./SPEC.md)（システム仕様書）／[SCREENS.md](./SCREENS.md)（画面設計書）

---

## 0. この文書について

### なぜ必要か

このシステムは**役割が2つ（クリニックオーナー／管理者）**あり、権限の境界・AIを使う長時間処理・外部サービス（OpenAI／Supabase Storage／Cloudflare D1）への依存が絡み合っています。

直近だけでも、次の不具合が**実際に画面を操作して初めて**発覚しました。

| 発覚した不具合 | 見つかった経緯 |
|---|---|
| サイト作成が4分かかり、Cloudflareの100秒制限で毎回接続が切れる | 作成ボタンを押して待った |
| 作り直すとDBのUNIQUE制約で**最後に必ず**失敗する（課金だけ発生） | 2回目の作成を試した |
| iPhone写真（HEIC）がアップロードは通るのに取り込みで弾かれる | 実際に画像を上げた |
| 診療時間が全角スペース区切りだと表が崩れる | 実データで確認した |

いずれも**コードを読むだけでは分からない**種類のものです。画面を一通り触る手順を決めておかないと、同じ見落としが繰り返されます。

### 記法

| 記号 | 意味 |
|---|---|
| 🔴 | **必須**。これが落ちたらリリース不可 |
| 🟡 | 重要。変更のたびに流す |
| ⚪ | 余裕があれば |
| 💰 | **OpenAIの課金が発生する** |
| ⏱ | 数分かかる |

### 検証状況について

⚠️ このテストケースのうち、**ログインを要するケースは未検証**です（作成者がアカウントにログインできないため）。ログイン不要の範囲（文言・権限拒否・SP表示・404）は実機で確認済みです。各章の冒頭に検証状況を記載しています。

---

## 1. 事前準備

| # | 準備するもの | 理由 |
|---|---|---|
| 1 | サーバー起動（`npm run build` → `npm start`） | 本番ビルドで確認する。`npm run dev` は表示が遅く、体感が実際と異なる |
| 2 | 管理者アカウント 1つ | D1 の `users` に `role='admin'` |
| 3 | **オーナーアカウント 2つ** | 「自分の申請」と「他人の申請」を分けるため。**2つ目は権限テストに必須** |
| 4 | テンプレート 3件（すべて「販売可」） | 0件だと自動選択が確認できない |
| 5 | テスト用画像 4種 | JPEG / PNG / **HEIC**（iPhone写真）/ **9MB超**の大きい画像 |

### 💰 課金が発生するケース

| ケース | 内容 | 目安 |
|---|---|---|
| A-13, A-14, A-18, A-21 | サイト作成（文章＋画像20枚） | 1回あたり数分 |
| A-31, A-32 | テンプレート作成 | 1回あたり30秒〜1分 |
| U-54 | セクションのAI書き直し | 1回あたり5秒 |

**回帰テストではサイト作成を1回だけ**にしてください。

### ⚠️ トンネル経由でテストする場合

URLに認証情報を埋め込まないでください。

```
❌ https://client:パスワード@xxxx.trycloudflare.com
⭕️ https://xxxx.trycloudflare.com  ← ダイアログに入力する
```

埋め込むと**保存・ログイン・画像アップロードなど、送信を伴う操作がすべて失敗**します（ブラウザの仕様）。

---

## 2. クリニックオーナーのテスト

### 2.1 アカウント（`/` `/signup`）

> 検証状況：エラー文言はソースと照合済み。画面操作（実際のログイン）は未実施。

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| U-01 | 🔴 | `/signup` で `test1@example.com` / `password123` / `password123` → 登録する | 登録され、自動ログインして `/home` へ |
| U-02 | 🔴 | 同じメールでもう一度登録 | **「このメールアドレスは既に登録されています。」** |
| U-03 | 🔴 | パスワードを `pass12`（6文字）で登録 | **「パスワードは8文字以上で入力してください。」** |
| U-04 | 🔴 | 確認欄だけ `password999` にして登録 | **「パスワードが一致しません。」** |
| U-05 | 🟡 | メール欄を空にして登録 | **「メールアドレスとパスワードを入力してください。」** |
| U-06 | 🔴 | `/` で正しい情報でログイン | `/home` へ遷移 |
| U-07 | 🔴 | パスワードを1文字変えてログイン | ログイン失敗のエラー。**入力したメールアドレスは消えない** |
| U-08 | 🔴 | オーナーでログイン中に `/admin` を開く | 管理者ダッシュボードに**入れない**（管理者ログイン画面のまま） |
| U-09 | 🟡 | ログアウト → ブラウザの「戻る」で `/home` | ログイン画面へ戻される（キャッシュで中身が見えない） |

### 2.2 新規申請（`/mypage/apply`）

> 検証状況：U-15・U-16 は実機で確認済み（下記の文言はサーバーの実際の応答）。他はソース照合。

10ステップのウィザードです。

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| U-11 | 🔴 | 画面上部のステップ表示 | `基本情報→写真→診療科→特徴→ターゲット→診療時間→スタッフ紹介→料金表→ご要望→申請` の**10段** |
| U-12 | 🔴 | クリニック名を空のまま「次へ」 | **「クリニック名を入力してください。」** が出て進めない |
| U-13 | 🔴 | 基本情報の入力欄 | クリニック名／住所／電話番号／LINE ID の**4つだけ**。**院長名の欄が無い** |
| U-14 | 🔴 | 写真ステップで JPEG と PNG を選ぶ | サムネイルが出る。エラーなし |
| U-15 | 🔴 | **HEIC**（iPhone写真）を選ぶ | 下記のメッセージが出て、**アップロードされない**<br>「〜はiPhoneの写真（HEIC形式）です。このままではホームページに表示できません。JPEG・PNG・WebP・GIF のいずれかで保存し直してからアップロードしてください。（iPhoneの場合、設定 → カメラ → フォーマット →「互換性優先」…）」 |
| U-16 | 🟡 | 9MB超の画像を選ぶ | **「〜のサイズが大きすぎます（8MBまで）。」** |
| U-17 | 🔴 | 診療時間に**全角スペース区切り**で入力<br>`月〜金　10時〜18時`<br>`土、日　休み` | 入力できる（表示の確認は U-41 と G-01） |
| U-18 | 🟡 | スタッフ2名を入力。コメントは**あえて崩した口語**で<br>例：`こどもがすきです。やさしく対応します` | 人数分のカードが増える |
| U-19 | 🟡 | 料金表を2件入力。**1件だけ備考**も入れる | 2行とも入力できる（列ずれの確認は G-04） |
| U-20 | 🔴 | ご要望に `お子さん連れでも入りやすい、明るい雰囲気にしたい` | 入力できる |
| U-21 | 🔴 | 「申請」ステップの確認表 | 入力した全項目が並ぶ。**「ご要望」の行がある**。**「院長名」の行が無い** |
| U-22 | 🔴 | 「申請する」 | `/mypage/requests` へ移動。状態が **「承認待ち」** |
| U-23 | 🟡 | クリニック名だけ入れて他は全部空で申請 | 申請できる（空欄の項目は生成時に非表示になる） |

### 2.3 申請一覧（`/mypage/requests`）

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| U-31 | 🔴 | 一覧を開く | **自分の申請だけ**が出る。もう1つのアカウントの申請は出ない |
| U-32 | 🔴 | 状態バッジ | 承認待ち（黄）／作成中（青）／生成済み（緑）／生成失敗（赤） |
| U-33 | 🟡 | 「詳細を確認」 | `/sites/<slug>` へ。戻るリンクが **「← 申請一覧へ戻る」** |
| U-34 | 🔴 | 削除 → キャンセル | 確認ダイアログが出て、キャンセルすると**消えない** |
| U-35 | 🔴 | 削除 → OK | 一覧から消える |
| U-36 | ⚪ | 申請0件の状態 | **「申請はまだありません。」** |

### 2.4 サイト詳細（`/sites/[slug]`）

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| U-41 | 🔴 | 生成済みサイトの詳細を開く | 診療時間が **`月〜金｜10時〜18時`／`土、日｜休み`** と**2列に分かれて**表示。1列に潰れていない |
| U-42 | 🔴 | ボタンを確認 | 「編集する」「新しいタブで開く」がある。**「AIで再生成する」は無い**。**「Cloudflareでプレビュー」は灰色で押せない** |
| U-43 | 🔴 | オーナーで**未生成**サイトの詳細 | 「まだ生成されていません。」のみ。**「作成」ボタンは出ない**（管理者専用）。※ページ上部の「**作成日時:**」は別物。文字列検索で確認する場合は `<button>` の中身だけを見ること |
| U-44 | 🔴 | **他人の** slug をURL直打ちして「編集する」 | 編集画面に入れず弾かれる |

### 2.5 サイト編集（`/sites/[slug]/edit`）

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| U-45 | 🔴 | 「編集する」 | 3ペイン。左＝ブロック一覧／中央＝プレビュー／右＝設定 |
| U-46 | 🔴 | プレビューの見出しをクリックして書き換え → 保存する | 「未保存の変更があります」が出る→保存後に消える。プレビューに反映。**待ち時間なし（AIを呼ばない）** |
| U-47 | 🔴 | ブロックをドラッグで並べ替え → 保存 | 順番が変わり、ページ内メニューの並びも変わる |
| U-48 | 🔴 | 👁 で非表示 → 保存 | そのセクションとメニュー項目が消える |
| U-49 | 🟡 | 「＋ブロックを追加」→ 自由文 → 保存 | 末尾に追加。**メインビジュアルとお問い合わせはグレーで選べない**（各1つまで） |
| U-50 | 🔴 | 画像フィールドで PNG に差し替え | サムネイルが変わり、保存後にプレビューにも反映 |
| U-51 | 🔴 | 画像フィールドで **HEIC** を選ぶ | U-15と同じ案内。**アップロードされない** |
| U-52 | 🟡 | セクションの余白部分（文字以外）をクリック | 右パネルが **「選択中: セクション全体」**。背景色／内側の余白／外側の余白が出る |
| U-53 | 🟡 | 背景色を変更 | **保存前でも**プレビューに即反映 |
| U-54 | 💰🟡 | セクションを選択 →「もっと親しみやすい…」→「AIで書き直す」 | 変更前後が項目ごとに並ぶ。**そのセクションだけ**変わる。「元に戻す」で完全復帰 |
| U-55 | 🔴 | 変更したまま「公開する」 | 押せない（**「先に保存してください」**） |
| U-56 | 🟡 | 保存 →「公開する」 | 公開URL（`*.pages.dev`）が緑枠で表示 |
| U-57 | 🔴 | 変更したままタブを閉じる | ブラウザが「変更が保存されていません」と警告 |
| U-58 | 🟡 | 「ガイドラインチェック」 | 医療広告ガイドラインの確認結果がモーダルで出る |

---

## 3. 管理者のテスト

### 3.1 ログインとダッシュボード

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| A-01 | 🔴 | `/admin` でログイン | `/admin/dashboard` へ |
| A-02 | 🔴 | `/admin` の画面 | **新規登録リンクが無い**（管理者は `/admin/users` からのみ作成） |
| A-03 | 🟡 | ダッシュボードの4枚のカード | ユーザー／リクエスト／テンプレート／部門の件数。押すと各画面へ |
| A-04 | 🔴 | 管理者で `/mypage/requests` を開く | オーナー画面には**入れない**（`/` へ） |

### 3.2 リクエスト管理（`/admin/requests`）★中心業務

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| A-11 | 🔴 | 一覧を開く | **全オーナーの申請**が出る（オーナー画面と違い、自分の分だけではない） |
| A-12 | 🔴 | ボタンの文言 | **「作成」**（旧「承認して生成」ではない） |
| A-13 | 🔴💰 | 承認待ちの行で「作成」 | **1秒以内に**「作成中…（数分）」へ変わり、状態バッジが **「作成中」** になる |
| A-14 | 🔴⏱ | A-13のまま待つ | 10秒ごとに**自動更新**され、数分後に「生成済み」へ。**手動リロード不要** |
| A-15 | 🔴⏱ | A-13の直後に**タブを閉じ**、5分後に開き直す | **作成は完走している**（「生成済み」）。※Cloudflareの100秒制限の回帰確認 |
| A-16 | 🔴 | 「作成中」の行 | **「作成」ボタンが出ない**（二重起動の防止） |
| A-17 | 🟡 | 生成済みの行 | 「編集」ボタン → `/sites/<slug>/edit` |
| A-18 | 🔴💰⏱ | **生成済みのサイトをもう一度作成** | UNIQUE制約エラーにならず完走する。※過去バグの回帰確認 |
| A-19 | 🟡 | 生成失敗した行 | 「作成」ボタンが**再度出る**（復旧できる） |
| A-20 | 🟡 | 削除 | 確認ダイアログ後に消える |

### 3.3 サイト詳細（管理者視点）

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| A-21 | 🔴💰 | **未生成**サイトの詳細 | **「作成」ボタンが出る**（オーナーには出ない） |
| A-22 | 🔴 | 戻るリンク | **「← リクエスト管理へ戻る」**（オーナーは「申請一覧へ戻る」） |
| A-23 | 🔴 | 「作成」を押した直後 | 青枠＋回転アイコンで<br>「AIがホームページを作っています。完了まで**5分ほど**かかります。この画面は自動で更新されます。**閉じても作成は続きます**…」 |
| A-24 | 🔴 | 他人のサイトの「編集する」 | 管理者は**全サイト編集できる** |

### 3.4 テンプレート管理（`/admin/templates`）

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| A-31 | 🔴💰⏱ | `/admin/templates/new` →「作成済みのサイトから作る」 | 数十秒で完了し編集画面へ。**元サイトと同じ配色・書体**になる |
| A-32 | 🟡💰⏱ | 「外部サイトのURLから作る」に実在サイトのURL | 30秒ほどで作成。抽出色が元サイトに近い |
| A-33 | 🔴 | URL・画像URLとも**空**で送信 | **「参考サイトのURLか、参考画像のURLのどちらかを入力してください。」** |
| A-34 | 🔴 | URLに `http://localhost:3000` | **「このホストは取得できません（localhost）。社内・ローカルのアドレスは指定できません。」** |
| A-35 | 🔴 | URLに `http://192.168.0.1` | 同上（社内アドレスの拒否） |
| A-36 | 🟡 | URLに `ftp://example.com` | **「http:// または https:// のURLを指定してください。」** |
| A-37 | 🟡 | 存在しないドメイン | **「ホスト名を解決できませんでした（〜）。」** |
| A-38 | 🔴 | 「非公開にする」→「販売可にする」 | バッジが「販売可（自動選択の候補）」⇄「非公開（候補に入らない）」 |
| A-39 | 🔴 | 「プレビュー」 | 別タブでテンプレートが表示される |
| A-40 | 🟡 | テンプレートを削除 | 確認ダイアログ後に消える |
| A-41 | 🔴 | テンプレート編集画面 | サイト編集と**同じ3ペイン**。ただし**「公開する」ボタンが無い** |
| A-42 | 🔴 | オーナーでテンプレート編集URLを直打ち | **「テンプレートを編集できるのは管理者のみです。」** |

### 3.5 マスタ管理

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| A-51 | 🔴 | `/admin/users` で新規ユーザー作成 | 一覧に追加される |
| A-52 | 🟡 | `/admin/users` の一覧 | **管理者アカウントは表示されない** |
| A-53 | 🔴 | `/admin/departments` で部門追加 → 部門名クリック → サービス追加 | 部門とサービスが登録される |
| A-54 | 🔴 | 直後にオーナーで `/mypage/apply` の診療科ステップ | **追加した部門・サービスが選択肢に出る** |
| A-55 | 🟡 | `/admin/features` `/admin/targets` で追加・編集・削除 | 一覧に反映。申請フォームの選択肢にも反映 |
| A-56 | ⚪ | `/admin/sections` | セクション種類の一覧が出る |

---

## 4. 生成されたサイトの中身

AIの出力が「嘘をついていないか」の確認です。**最も重要な章**です。

> 検証状況：G-01・G-04 は のじま歯科クリニック で実機確認済み。

| ID | 優先 | 確認箇所 | 期待結果 |
|---|---|---|---|
| G-01 | 🔴 | 診療時間の表 | U-17の入力が **`月〜金｜10時〜18時`** と2列に分かれている |
| G-02 | 🔴 | スタッフ紹介の文章 | U-18の口語コメントが**です・ます調に整えられている**。かつ**資格・経験年数・出身校など、入力に無い事実が足されていない** |
| G-03 | 🔴 | 電話番号・住所・LINE | **申請で入力した値と完全一致**（AIが創作していない）。※LINE IDは `@example` → `line.me/R/ti/p/%40example` と**URLエンコードされる**のが正常。生の文字列で検索すると一致しないので注意 |
| G-04 | 🔴 | 料金表 | 金額が入力どおり。**項目名と金額が2列で揃っている**（備考のある行だけ列がずれていない） |
| G-05 | 🔴 | 空欄にした項目（料金表・スタッフ紹介・診療時間・アクセス） | そのセクションごと**非表示**（見出しだけ残っていない） |
| G-05b | 🟡 | 空欄にした**お知らせ・よくある質問** | ⚠️ **これらは非表示にならない**。実データが無い場合のみAIが一般的な内容を書いてよい、という仕様（[SPEC.md](./SPEC.md) の HONESTY_RULES 2番目）。内容が医院の実態と矛盾しないかを目視で確認する |
| G-06 | 🟡 | ご要望の反映 | U-20の「明るい雰囲気」が、選ばれたテンプレートや文章に表れている |
| G-07 | 🟡 | 「このデザインを選んだ理由」 | 詳細画面に1文で表示される |
| G-08 | 🔴 | 画像 | 20枚前後が生成され、**壊れた画像やプレースホルダーのままの箇所が無い** |

---

## 5. スマートフォン表示（幅375〜390px）

> 検証状況：**全項目を幅390pxで実機確認済み**（2026-08-23 / のじま歯科クリニック）。

| ID | 優先 | 確認箇所 | 期待結果 |
|---|---|---|---|
| S-01 | 🔴 | ヘッダー | **電話番号が表示されない**（ロゴ＋医院名＋ハンバーガーのみ） |
| S-02 | 🔴 | 横スクロール | **発生しない** |
| S-03 | 🔴 | メインビジュアル | 文字がヘッダーや次のセクションに**はみ出さない** |
| S-04 | 🔴 | 予約ボタン（LINE／電話） | 縦に並び、**同じ枠内の2つは幅が完全に一致**。※ヒーローと問い合わせで枠が2組あり、組をまたいだ幅の違いは正常（実測 358/358 と 351/351） |
| S-05 | 🟡 | お知らせ | 日付とタイトルが**全項目とも縦積み**（項目ごとに配置が変わらない） |
| S-06 | 🟡 | 料金表 | 項目名と金額が読める。はみ出さない |
| S-07 | 🟡 | メニュー | ハンバーガーで開閉。リンクを押すと自動で閉じる |

**開発者向け**：ブラウザのコンソールで一括確認できます。

```js
({
  横スクロール: document.documentElement.scrollWidth > innerWidth,
  ヘッダー電話: getComputedStyle(document.querySelector('.header-tel')).display,
  ボタン幅: [...document.querySelectorAll('.cta-buttons .btn')].map(b => b.getBoundingClientRect().width),
})
```

---

## 6. 権限の境界

> 検証状況：**未ログイン列は実機確認済み**。ログインを要する列は未検証。

各セルについて「URLを直接入力して開く」で確認します。

| URL | 未ログイン | オーナー（自分の分） | オーナー（他人の分） | 管理者 |
|---|---|---|---|---|
| `/home` `/mypage/*` | ログイン画面へ | ✅ | — | `/` へ |
| `/admin/*` | 管理者ログインへ | `/admin` へ | — | ✅ |
| `/sites/[slug]` | ⚠️ **見えてしまう** | ✅ | ⚠️ **見えてしまう** | ✅ |
| `/sites/[slug]/edit` | 弾く | ✅ | **弾く** | ✅ |
| `/admin/templates/[id]` | 弾く | **弾く** | — | ✅ |
| `/create` `/sites` | **404** | **404** | **404** | **404** |

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| P-01 | 🔴 | 上表の全セルを確認 | 表のとおり。未ログイン時の遷移先は `/mypage/*` `/home` → `/`、`/admin/*` `/sites/[slug]/edit` → `/admin` |
| P-02 | 🔴 | `/create` と `/sites` を開く | **404**（削除済み） |
| P-03 | 🔴 | `/sites/[slug]` を未ログインで開く | ⚠️ **見えてしまう。これは既知の未対応**。認証を追加するまで Basic認証（`PREVIEW_BASIC_AUTH`）で全体を覆う運用 |

---

## 7. 異常系・復旧

| ID | 優先 | 操作 | 期待結果 |
|---|---|---|---|
| E-01 | 🔴 | 生成中にネットワークを切る | **「生成中に通信が切れました（AI画像の生成に時間がかかるため…）。もう一度「作成」を押してください。」** と日本語で表示。`fetch failed` のような英字だけにならない |
| E-02 | 🟡 | `OPENAI_API_KEY` を無効値にして作成 | 失敗理由が画面に出る。**「作成」ボタンで再実行できる** |
| E-03 | 🟡 | `public/generated/<slug>` を手で削除して詳細を開く | 「生成ファイルが見つかりません」と表示され、作り直せる |
| E-04 | 🟡 | 同じサイトの編集画面を2タブで開き、別々に保存 | 後から保存した方が残る（**既知の仕様**。警告は出ない） |
| E-05 | ⚪ | `FATAL: An unexpected Turbopack error` が出る | `npm run clean` 後に再起動で解消 |

---

## 8. 回帰テスト（最小セット・15分）

変更のたびに流す短縮版です。**🔴のうち課金が発生しないもの**に絞っています。

### 未ログインで確認（3分）

| 確認 | 期待 |
|---|---|
| `/create` `/sites` | 404 |
| `/mypage/requests` | ログイン画面へ |
| `/admin/dashboard` | 管理者ログイン画面へ |
| `/sites/[slug]/edit` | 弾かれる |

### オーナーで確認（6分）

| 確認 | 期待 |
|---|---|
| U-03（8文字未満） | 「パスワードは8文字以上で…」 |
| U-11（ステップ数） | 10段、`ご要望` を含む |
| U-13（基本情報） | 院長名の欄が無い |
| U-15（HEIC） | 対処法つきで拒否 |
| U-42（詳細画面） | 再生成ボタン無し／Cloudflareボタン無効 |
| U-46（文字編集→保存） | 待ち時間なしで反映 |
| U-50（PNG差し替え） | 反映される |

### 管理者で確認（4分）

| 確認 | 期待 |
|---|---|
| A-12（ボタン文言） | 「作成」 |
| A-21（未生成の詳細） | 「作成」ボタンが出る |
| A-33〜A-35（URL検証） | それぞれのエラー文言 |
| A-42（オーナーでテンプレート編集） | 「テンプレートを編集できるのは管理者のみです。」 |

### 生成済みサイトの表示（2分）

| 確認 | 期待 |
|---|---|
| G-01（診療時間） | 2列に分かれている |
| G-04（料金表） | 列が揃っている |
| S-01〜S-04（SP幅） | 電話番号なし／横スクロールなし／ボタン幅一致 |

---

## 9. 自動テストについて

現在このプロジェクトにテストフレームワークは入っていません（`package.json` に `test` スクリプトなし）。本書は**人が画面を触って確認する手順書**です。

将来自動化する場合の向き不向き：

| 章 | 自動化 | 理由 |
|---|---|---|
| 6章（権限の境界） | ⭕️ **向く** | HTTPステータスの確認だけで済む |
| 5章（SP表示） | ⭕️ **向く** | 寸法を数値で比較できる |
| 2章・3章（画面操作） | 🔺 一部可能 | ログイン状態の準備が必要 |
| 3.2章・4章（AI生成） | ❌ **向かない** | 1回数分＋課金。出力も毎回変わる |

---

## 10. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-23 | 初版作成。オーナー33件・管理者29件・生成物8件・SP7件・権限3件・異常系5件 |
